# JavaScript Fundamentals

A structured collection of practical code examples and notes documenting my JavaScript journey, from basic syntax to asynchronous programming and streaming patterns.

---

## 📂 Repository Contents

* `variables.js` – Variable declarations (`let`, `const`, `var`), scoping, and data types.
* `conditionals.js` – Control flow using `if`, `else if`, `else`, and `switch` statements.
* `ternary.js` – Concise conditional operations using ternary operators.
* `loops.js` – Iteration techniques (`for`, `while`, `for...of`, `for...in`).
* `errorhandling.js` – Managing exceptions with `try`, `catch`, `finally`, and custom errors.
* `async.js` – Asynchronous patterns, Promises, and `async`/`await`.
* `streamingresponses.js` – Handling readable streams and chunked data transfers.

---

## 🚀 Getting Started

### Prerequisites
Make sure you have [Node.js](https://nodejs.org/) installed on your machine.

### Installation & Execution
1. Clone the repository:
   ```bash
   git clone [https://github.com/ManjunathAnde/JavaScript-fundamentals.git](https://github.com/ManjunathAnde/JavaScript-fundamentals.git)
   cd JavaScript-fundamentals
